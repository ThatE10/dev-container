# ── Visualization ─────────────────────────────────────────────────────────────
plotly
seaborn

# ── Notebooks & interactive ───────────────────────────────────────────────────
jupyterlab
ipykernel
ipywidgets
nbformat
marimo

# ── Tensor utilities ─────────────────────────────────────────────────────────
einops

# ── SAE research ──────────────────────────────────────────────────────────────
# Pinned to the pre-transformers-5 line. sae-lens 6.x requires transformers 5+
# which would force upgrading torch/transformers away from the vLLM base image.
sae-lens<6

# ── Fine-tuning / training (not shipped in vLLM base) ────────────────────────
# Pinned to versions released against torch 2.5 / transformers 4.46 era.
# Later releases (peft 0.19, trl 1.7) require newer torch/transformers and
# will fight constraints.txt.
peft<0.15
trl<1.0

# ── Infrastructure ────────────────────────────────────────────────────────────
modal
python-dotenv

# ── Dev tools ─────────────────────────────────────────────────────────────────
black
ruff
pytest
pytest-asyncio
rich
tqdm
typer
httpx
